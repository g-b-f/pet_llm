# Pet LLM

Creates a pet LLM that can move around and think to itself.

To use, simply run `main.py`.
On the first time, the model will automatically download from huggingface,
taking a few minutes. You can speed up the download by putting a token in `models/hf_token.json`.